# 🤖 Telegram Bot для Печенье Судьбы

Простой бот для обработки команд `/start` и `/help` с кнопкой запуска Mini App.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd bot
npm install
```

### 2. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env`:
```
BOT_TOKEN=8517101731:AAHGh6BvwWdJyKWTC_pgn-QVf4OSYuvVH6k
WEB_APP_URL=https://your-app.vercel.app
```

### 3. Запуск бота

```bash
npm start
```

Или для разработки с автоперезагрузкой:
```bash
npm run dev
```

## 📦 Деплой на VPS или хостинг

### Вариант 1: Railway (Рекомендуется - бесплатно)

1. Перейдите на [railway.app](https://railway.app)
2. Войдите через GitHub
3. New Project → Deploy from GitHub repo
4. Выберите репозиторий и папку `bot`
5. Добавьте переменные окружения:
   - `BOT_TOKEN`
   - `WEB_APP_URL`
6. Deploy

### Вариант 2: Render

1. Перейдите на [render.com](https://render.com)
2. New → Web Service
3. Подключите GitHub репозиторий
4. Настройки:
   - Build Command: `cd bot && npm install`
   - Start Command: `cd bot && npm start`
5. Добавьте переменные окружения
6. Deploy

### Вариант 3: Heroku

```bash
heroku create your-bot-name
heroku config:set BOT_TOKEN=your_token
heroku config:set WEB_APP_URL=your_app_url
git push heroku main
```

## 🔧 Настройка

После деплоя бота, обновите `WEB_APP_URL` в переменных окружения на реальный URL вашего Mini App с Vercel.

## 📝 Команды бота

- `/start` - приветственное сообщение с кнопкой запуска Mini App
- `/help` - справка по боту

