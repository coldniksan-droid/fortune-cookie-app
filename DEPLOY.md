# 🚀 Инструкция по деплою на Vercel и подключению к Telegram Bot

## 📦 Шаг 1: Деплой на Vercel

### Вариант A: Через веб-интерфейс Vercel (Рекомендуется)

1. **Перейдите на [vercel.com](https://vercel.com)**
   - Войдите через GitHub аккаунт

2. **Импортируйте проект:**
   - Нажмите "Add New Project"
   - Выберите репозиторий `fortune-cookie-app`
   - Vercel автоматически определит настройки

3. **Настройки проекта:**
   - **Framework Preset:** Vite
   - **Build Command:** `npm run build`
   - **Output Directory:** `dist`
   - **Install Command:** `npm install`

4. **Деплой:**
   - Нажмите "Deploy"
   - Дождитесь завершения деплоя
   - Скопируйте URL вашего приложения (например: `https://fortune-cookie-app.vercel.app`)

### Вариант B: Через Vercel CLI

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Войдите в Vercel
vercel login

# Деплой
vercel

# Для production деплоя
vercel --prod
```

## 🤖 Шаг 2: Подключение к Telegram Bot Father

1. **Создайте бота:**
   - Откройте [@BotFather](https://t.me/BotFather) в Telegram
   - Отправьте команду `/newbot`
   - Следуйте инструкциям:
     - Введите имя бота (например: "Печенье Судьбы")
     - Введите username бота (например: `FortuneCookieBot`)
   - Сохраните **Bot Token** (он понадобится позже)

2. **Создайте Mini App:**
   - Отправьте команду `/newapp`
   - Выберите созданного бота
   - Введите название приложения (например: "Печенье Судьбы")
   - Введите описание (например: "Узнай своё предсказание дня!")
   - Введите **URL вашего приложения** с Vercel (например: `https://fortune-cookie-app.vercel.app`)
   - Загрузите фото для иконки (опционально)
   - Загрузите фото для обложки (опционально)

3. **Настройте кнопку меню:**
   - Отправьте команду `/myapps`
   - Выберите ваше приложение
   - Нажмите "Edit" → "Menu Button"
   - Введите текст кнопки (например: "🔮 Узнать предсказание")
   - Сохраните

## ⚙️ Шаг 3: Обновите настройки в коде

После получения URL от Vercel, обновите следующие файлы:

### 1. `src/App.jsx` - Обновите имя бота:

Найдите строку:
```javascript
const BOT_USERNAME = 'YOUR_BOT_USERNAME';
```

Замените на:
```javascript
const BOT_USERNAME = 'FortuneCookieBot'; // Ваше реальное имя бота
```

### 2. `src/App.jsx` - Обновите Adsgram Block ID:

Найдите строку:
```javascript
const adsgramBlockId = "YOUR_BLOCK_ID";
```

Замените на реальный ID из Adsgram (если используете рекламу).

## 🧪 Шаг 4: Тестирование

1. **Откройте вашего бота в Telegram**
2. **Нажмите на кнопку меню** (если настроили)
3. **Или отправьте команду** `/start` и нажмите на кнопку приложения
4. **Проверьте работу:**
   - Тап по печенью
   - Показ предсказания
   - Кнопка "Поделиться в историю"
   - Кнопка "Разблокировать секретное предсказание"

## 📝 Дополнительные настройки

### Настройка домена (опционально)

Если хотите использовать свой домен:
1. В Vercel перейдите в Settings → Domains
2. Добавьте ваш домен
3. Следуйте инструкциям по настройке DNS

### Настройка переменных окружения (если нужно)

В Vercel Dashboard:
1. Settings → Environment Variables
2. Добавьте необходимые переменные (например, API ключи)

## 🔄 Обновление приложения

После каждого изменения в коде:

```bash
# Закоммитьте изменения
git add .
git commit -m "Описание изменений"
git push origin main

# Vercel автоматически задеплоит новую версию
```

## ❓ Решение проблем

### Приложение не открывается в Telegram
- Проверьте, что URL в Bot Father правильный
- Убедитесь, что URL начинается с `https://`
- Проверьте, что деплой на Vercel завершен успешно

### Ошибки в консоли
- Откройте DevTools в браузере (F12)
- Проверьте вкладку Console на наличие ошибок
- Убедитесь, что все файлы загружаются (Network tab)

### Реклама не показывается
- Проверьте, что Adsgram Block ID указан правильно
- Убедитесь, что реклама настроена в панели Adsgram

---

**Готово! 🎉** Ваше приложение теперь доступно в Telegram!

